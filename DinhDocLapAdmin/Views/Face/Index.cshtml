@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "Face";
    ViewData["Slug1"] = "QUẢN LÝ FACE";
    ViewData["Slug2"] = "Quản lý Face";

    Pager pager = new Pager();
    int pageNo = 0;
    if (ViewBag.Pager != null)
    {
        pager = ViewBag.Pager;
        pageNo = pager.currentPage;
    }

}
<style>
    td {
        text-align: center;
    }
</style>
@model List<Face>
<div class="table-responsive" id="face">
    <table class="table" style="margin-top: 20px; background-color:white; ">
        <tr>
            <td colspan="2">             
             </td>

            <td>
                <a asp-controller="face" asp-action="insert" class="btn btn-primary">Thêm</a>
            </td>
        </tr>
        <tr style="background-color: #b4dca4; ">
            <td><strong>ID Face</strong></td>
            <td><strong>Tên Face</strong></td>
            <td></td>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.IDF</td>
                <td>@item.faceName</td>
                <td>
                    <a class="btn btn-warning" asp-controller="face" asp-action="update" asp-route-id="@item.IDF">Cập nhật</a>
                </td>
            </tr>
        }
    </table>
</div>
<nav aria-label="Page navigation example" style="float: right;">
    <ul class="pagination justify-content-center">
        @for (var pge = pager.startPage; pge <= pager.endPage; pge++)
        {
            <li class="page-item @(pge==pager.currentPage ? "active" : "")">
                <a class="page-link" asp-controller="face"  asp-action="Pagination" asp-route-pg="@pge" >@pge</a>
            </li>
        }
    </ul>
</nav>